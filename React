<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
    <input type="text"> </input>
     <button onclick="createele()">Add</button>
</body>
<script>


function createele(){
 
    const Value  = document.querySelector("input").value;
    const span = document.createElement("span"); // create a span tag 
    const button = document.createElement("button");
    span.innerHTML = Value;
    button.innerHTML = "Delete";
    const div = document.createElement("div");
    div.appendChild(span);
    div.appendChild(button);
    document.querySelector("body").appendChild(div);
}
</script>
</html>
// pros Handling 
const Name = (props) => {
  // const { frist,color } = props;
  const {info}= props;
  return (
    // <p>{frist},{color}</p>
    <p>I am {info.brand}</p>
  );
}

export default Name;


// pros sent to another componet 

import React from "react";
import Car from "./Car";
function App  () {

  const name = "hello World";
  const color = "Black"
  const info = {brand:'Redmi', Model : '15 pro Max'};
  return (
    //  <Car frist = {name} color= {color} info/>
    <Car info ={info} />
  );

}

export default App;

// ternary operator used only in if else condtion

// && operator only used in if condition 

lisiting 
// Object using object unique  Value
// array access index Value 



useffect 

no dependice = > every time run
[] = one time Run 
[ value ] = Value Change after Run 
